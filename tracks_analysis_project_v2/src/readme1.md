🚀 КАК ЗАПУСТИТЬ:
1. Настройка PostgreSQL
bash
# Установить PostgreSQL если нет
sudo apt-get install postgresql

# Запустить PostgreSQL
sudo service postgresql start

# Залогиниться
sudo -u postgres psql

# Создать базу данных
CREATE DATABASE tracks_db;

# Создать пользователя (опционально)
CREATE USER myuser WITH PASSWORD 'password';
GRANT ALL PRIVILEGES ON DATABASE tracks_db TO myuser;
2. Установить зависимости
bash
pip install -r requirements.txt
requirements.txt:

text
streamlit
flask
pandas
numpy
plotly
scikit-learn
sqlalchemy
psycopg2-binary
joblib
gpxpy
openmeteo-requests
requests-cache
retry-requests
folium
3. Запустить в 4 терминалах:
Терминал 1 - обработка GPX:

bash
python main.py
(предварительно положи GPX файлы в папку ./data/)

Терминал 2 - обучение моделей:

bash
python analysis.py        # кластеризация
python ml_train.py        # классификация
Терминал 3 - API сервер:

bash
python api.py
Терминал 4 - приложение:

bash
streamlit run streamlit_app.py
Терминал 5 (опционально) - дашборд:

bash
streamlit run dashboard.py
4. Открыть в браузере:
Приложение для ввода: http://localhost:8501

Дашборд: http://localhost:8502 (если запустил)

API: http://localhost:5000

📁 СТРУКТУРА ПРОЕКТА:
text
gps-tracker/
├── data/                   # GPX файлы
├── outputs/
│   ├── models/            # Сохраненные модели
│   └── maps/              # Карты
├── main.py                # Обработка GPX → БД
├── analysis.py            # Кластеризация (у тебя есть)
├── ml_train.py            # Классификация
├── api.py                 # Flask API
├── streamlit_app.py       # Приложение для ввода
├── dashboard.py           # Дашборд (у тебя есть)
└── requirements.txt       # Зависимости
✅ ЧТО РАБОТАЕТ:
Обработка GPX → сохранение в БД

Кластеризация (KMeans, DBSCAN) → сохранение моделей

Классификация (3 алгоритма) → выбор лучшей модели

API с эндпоинтами для предсказаний

Приложение для ввода своих данных

Дашборд для анализа треков из БД

Все тянет из PostgreSQL

Тестируй! 🚀